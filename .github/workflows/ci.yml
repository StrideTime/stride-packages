name: CI

on:
  pull_request:
    branches: [main, beta, alpha]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [24.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          registry-url: "https://npm.pkg.github.com"

      - name: Configure npm registry
        run: |
          echo "@stridetime:registry=https://npm.pkg.github.com" >> .npmrc
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}" >> .npmrc

      - name: Install dependencies
        run: yarn install
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit lockfile if changed
        run: |
          if [ -f yarn.lock ] && [ -n "$(git status --porcelain yarn.lock)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add yarn.lock
            git commit -m "chore: update yarn.lock"
            git push
          fi

      - name: Lint code
        run: |
          if [ -d "packages" ] && [ "$(ls -A packages 2>/dev/null)" ]; then
            yarn lint
          else
            echo "No packages found, skipping lint"
          fi

      - name: Type check
        run: |
          if [ -d "packages" ] && [ "$(ls -A packages 2>/dev/null)" ]; then
            yarn typecheck
          else
            echo "No packages found, skipping typecheck"
          fi

      - name: Run tests
        run: |
          if [ -d "packages" ] && [ "$(ls -A packages 2>/dev/null)" ]; then
            yarn test
          else
            echo "No packages found, skipping tests"
          fi

      - name: Build packages
        run: |
          if [ -d "packages" ] && [ "$(ls -A packages 2>/dev/null)" ]; then
            yarn build
          else
            echo "No packages found, skipping build"
          fi
